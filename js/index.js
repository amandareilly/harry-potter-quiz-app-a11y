function renderStartPage() {
  //render the start page
}

function renderQuizPage(gameData) {
  //renders the shell of the quiz page after the user clicks "start"
}


function renderQuestion(question) {
  //renders a question on a quiz page
}

function renderAnswer() {
  //renders an answer on a quiz page
}

function updateQuizData() {
  //updates the score and current question number in the game header
}

function renderFinalResults() {
  //render the final results page
}
